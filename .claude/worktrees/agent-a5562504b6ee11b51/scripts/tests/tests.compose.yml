services:
  mr-review-tests:
    image: mr-review-api:tests-local
    build:
      context: ../../services/mr-review
      dockerfile: Dockerfile.api
      target: test
    restart: "no"
    profiles: ["mr-review-tests", "all"]

networks:
  mr-review-test-network:
    driver: bridge
