[project]
name = "mr-review-docs"
version = "0.1.0"
description = "Documentation build tooling for mr-review"
requires-python = ">=3.12"

[dependency-groups]
docs = [
    "zensical>=0.0.37",
]

[tool.uv]
link-mode = "copy"
