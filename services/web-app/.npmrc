# Nexus npm-proxy: keep concurrency very low — many parallel .tgz pulls still overload proxy → ERR_SOCKET_TIMEOUT.
network-concurrency=2
fetch-timeout=900000
fetch-retries=8
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
